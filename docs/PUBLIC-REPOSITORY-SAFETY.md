# Public repository safety

This repository is safe for public publication. Production host addresses,
Telegram owner identifiers, credentials, and other deployment-only values are
kept outside the repository and are represented by placeholders or redaction
markers in public evidence.

The VPS installation remains configured independently under `/etc/hermes-factory`.
Do not copy public example values into a production configuration without
reviewing the owner-specific deployment settings.

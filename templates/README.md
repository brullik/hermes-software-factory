# Templates

Примеры показывают форму контрактов, а не реальные IDs, credentials или deployment values. Runtime обязан генерировать новые IDs/digests и валидировать каждый artifact соответствующей schema.
